# 精灵切割正确性 & 动画稳定性 —— 失败模式清单与修复方案

> 目的：把"切图不准（剑尖被切到下一帧）"和"动画闪烁/跳动"放进**同一份设计依据**里，
> 避免分开打地鼠。所有条目都指向 `internal/sprite/` 的具体代码行。
>
> 核心判断：
> - **A 组（切割正确性）**：根因是「一维投影 + 竖直直线刀 + 丢弃连通信息」。
> - **B 组（动画稳定性）**：根因是「归一化用的锚点全都会随肢体/武器/特效移动」。
> - 两组**上下游耦合**：A 组的单帧切割误差会喂给 B 组的全部归一化器，放大成单帧 glitch。
>
> **验证状态（2026-06-22，用合成测试逐条复现）**：
> - ✅ **A1 已修复**：连通感知归属（`extract.go` `labelComponents`/`assignComponentOwners`/`extractOwnedContent`）。回归测试 `TestSwordStaysWithItsPose`：无剑的 B 帧剑像素 534→0。真实 `attack-east` 长条：frame3 的 773px 游离碎片 →0，frame2 的剑保持完整。原始复现量化为右段抢走左 pose **455 个剑像素**。
> - ⚠️ **B1/B2 已回退（首次尝试失败）**：用「共享 scale = `bodyExtent`(80% 质量范围)」做统一缩放，但 80% 质量高度**低估真实身体高度**（头/脚/小腿质量低被排除）→ 缩放高估 → 全身超格 → 脚锚定在基线、顶部被 `top=0` 夹住 → 身体从底部被切（idle「只剩膝盖以上」，攻击帧也被截）。合成测试 Δ=0 但漏掉了「身体不得被裁」这条不变量，合成 body 恰好不触边所以没暴露。已回退到逐帧 fit（有脉动但永不裁切身体）。
>   - **正确方向**：用**腐蚀（erosion）**得到「排除细长武器后的真实身体框」，缩放只需让身体框入格、细剑超格裁边。需要补一条回归测试断言**输出帧身体高度 = 完整身体**（用一个头/脚细、躯干粗的合成 idle 体复现裁切），并在**真实 idle 长条**上验证后再重新合入。
> - ❌ **B4（独立基线漂移）已证伪、撤回**：构造「某帧发下垂剑尖」后，各帧身体 top **Δ=0**，基线对齐是自洽的。先前看到的垂直晃动是 **B1 的缩放脉动泄漏到垂直方向**，并非独立机制 → 归入 B1 一并修。
> - ⏳ B3/B5/B6 未单独隔离：B3 是 B1+B2 的后果，B5/B6 被 B1 主导，**待 B1 修复后再评估**，不预先写修复。

---

## A 组：切割正确性

| ID | 症状 | 根因（代码位置） | 修复方向 | 优先级 |
|----|------|------------------|----------|--------|
| **A1** | 剑尖/拖尾被切到下一帧（已复现，量化为 455 像素 bleed） | 投影分割只产生**竖直列切线**（`segment.go` `dpNCut`/`splitRange`），`extractContent`（`extract.go:23`）把 `[start,end)` 内**所有**像素一股脑 bbox，丢弃连通归属 | **连通感知归属**：投影照旧定「几个 pose、大概在哪」，但边界上**跨切线的连通块整块判给其躯干峰所属的 pose**（用 `posePeaks`） | P0 |
| **A2** | 武器越横跨缝隙、DP 越爱从剑中间下刀（加重 A1） | 武器 alpha 填平了 gutter → `contentRuns`（`segment.go:60`）找不到谷 → 走 `dpNCut`，而它专挑「质量最小列」= 剑身列 | 同 A1；或把直线刀升级为**弯曲 min-cut 接缝（seam carving）**，让切缝绕开剑身 | P1 |
| **A3** | 真·物理重叠（剑尖压在下个角色身上、零缝隙）无法本地分离 | 任何本地算法都救不了：像素本身就重叠 | **检测并自动重生成**（已有 `natural!=expected` 警告，`extract.go:164`，升级为自动重试）；或高质量档**一帧一图**不切 | P2 |
| **A4** | 切割正确性**零测试覆盖** | 现有 `TestSegmentForceExpectedOnOverlap` 只断言 `natural==2`，全部 segment/extract 测试只测**数量/质心/缩放**，无一测**像素归属** | 落正式回归测试：断言「右段不得含属于 A 的剑像素」（已有复现脚本，量化 455px） | P0 |

---

## B 组：动画稳定性（即使切割 100% 正确仍会抖）

| ID | 症状 | 根因（代码位置） | 修复方向 | 优先级 |
|----|------|------------------|----------|--------|
| **B1** | 躯干随挥剑**一呼一吸胀缩**（最刺眼） | 共享 `scale` 基于身体 extent（`extract.go:116`），但 `:124` 又按**含武器的整 bbox** 逐帧 `boxScale=min(scale, availW/img.W, availH/img.H)` clamp → 宽帧被额外缩小 | **真正统一缩放**：全序列用同一 scale（身体 extent 的稳健分位数），不逐帧 clamp；超格允许裁边而非缩小整人 | P0 |
| **B2** | 身体被武器"拽"得**左右滑** | 水平用 alpha 质心 `cx` 居中（`extract.go:148`）；剑右挥→质心右移→身体被推左 | 锚点换成**躯干/胯部质心**（剔除细长肢体后的质量中心，复用 `bodyExtent` 思路） | P0 |
| **B3** | 过宽帧**突然弹到格子边再弹回**（硬 pop） | `left` 越界 clamp（`extract.go:149-154`）：多数帧质心居中，过宽帧贴边 → 位置不连续 | 同 B1（统一缩放后过宽消失）+ B2（稳定锚点后偏移更小）；clamp 改为软约束/允许裁边 | P1 |
| ~~**B4**~~ | ~~脚没动却上下浮沉~~ **（已证伪，撤回）** | 测试构造「下垂剑尖」后各帧 body top Δ=0，基线对齐自洽。垂直晃动实为 **B1 缩放脉动泄漏到垂直**，非独立基线漂移 | 归入 **B1**（统一缩放后垂直自动稳定） | ~~P0~~ 撤回 |
| **B5** | 即使一切完美仍有 **±1px 逐帧抖** | `left/top/sw/sh/contentBaseline` 全 `int(...+0.5)` 取整（`extract.go:129,143,148,155`） | **亚像素对齐**（保留小数靠重采样）或**锁整数缩放**消除取整跳变 | P1 |
| **B6** | 轮廓**蠕动/闪边**（shimmer） | `CatmullRom.Scale` 逐帧平滑重采样（`extract.go:140`）+ B1 让每帧 boxScale 都不同 → 边缘抗锯齿落点每帧不同 | 统一 scale（治 B1）后 shimmer 大减；像素画考虑**整数缩放 + 最近邻/量化后对齐** | P1 |
| **B7** | 切割单帧误差→该帧 bbox/质心/extent 全变→单帧 glitch | A 组误差是 B 组的上游输入（`extract.go` 全程依赖每帧 content 的几何） | 先修 A 组；B 组锚点稳健化后对单帧异常更不敏感 | 依赖 A |
| **B8** | 位置/尺寸抖动**零检测** | `inspect.go` 的 drift 只看**颜色直方图**（`:19-20`, `:164`），管「角色变没变」不管「位置/大小抖没抖」 | 同轴补一个**质心轨迹 + 高度 + 缩放平滑度**检测器，超阈值警告/重生成（兜底） | P2 |

---

## 本质归纳

> **精灵动画的正确参照系 = 稳定的骨骼锚点（脚底 / 胯部 / 躯干质心），而非 bbox、也非 alpha 质心。**

当前代码的三个锚点全会随手里那把剑动，所以全在抖：

| 锚点 | 当前用法（代码） | 驱动了什么 | 为何抖 |
|------|------------------|-----------|--------|
| bbox 宽高 | `extract.go:124` boxScale | 缩放 | 含武器 extent，逐帧变 → B1/B6 |
| alpha 质心 | `extract.go:148` cx | 水平位置 | 武器把质心拽偏 → B2/B3 |
| 最低像素 | `extract.go:96` baseline | 垂直位置 | 下垂特效拉低下沿 → B4 |

把这三个换成**对肢体/武器/特效鲁棒的解剖锚点**，B1–B6 一并收敛。

---

## 建议实施顺序

1. **P0 切割地基**：A4（落回归测试，TDD 锚点）→ A1（连通感知归属）。
2. **P0 稳定地基**：B1（统一缩放）+ B2（躯干质心水平锚）+ B4（稳健脚底基线）。这三者共用「鲁棒锚点」抽象，一起做最省。
3. **P1 打磨**：A2（弯曲接缝，可选）、B3/B5/B6（pop / ±1px / shimmer）。
4. **P2 兜底**：A3（自动重生成 / 一帧一图档）、B8（位置-尺寸 drift 检测器）。

> 关键耦合提醒：**先 A 后 B**。B 组锚点鲁棒化能吸收 A 组的残余误差，但若 A 组还在大幅误切，再稳的锚点也压不住单帧 glitch（B7）。
